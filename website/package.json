{
  "name": "seedstone-website",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --filter seedstone build",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "pregenerate": "pnpm --filter seedstone build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "nuxt": "npm:nuxt-nightly@5x",
    "seedstone": "workspace:*",
    "shiki": "^4.2.0",
    "vue": "^3.5.35",
    "vue-router": "^5.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
